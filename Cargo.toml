[package]
name = "something"
version = "0.1.0"
edition = "2024"

[dependencies]
noto-sans-mono-bitmap = { version = "0.3", features = ["size_16", "regular"] }
spin = "0.12.0"
uefi = "0.37.0"

[profile.release]
panic = "abort"
